# 🚀 KREATIV AI (COURSE ACADEMY) — LANDING PAGE MASTER CONTEXT & CODEBASE

> **Ushbu fayl nima uchun?**
> Bu fayl zamonaviy, konversiyasi yuqori va ko'zni qamashtiradigan yangi **Landing Page** yaratish uchun har qanday AI (v0.dev, Lovable, Bolt.new, Cursor, Claude 3.7, ChatGPT) ga to'g'ridan-to'g'ri berish uchun tayyorlangan to'liq kontekst, matnlar, xususiyatlar va mavjud frontend kodlari jamlanmasidir.

---

## 1. LOYIHA HAQIDA VA POZITSIYALANISH (BRAND IDENTITY)

* **Loyiha nomi:** Kreativ AI (yoki Course Academy)
* **Yo'nalish:** Sun'iy intellekt (AI), SMM, Canva & Grafik dizayn, Prompt Engineering va zamonaviy raqamli kasblar platformasi.
* **Platforma formati:** Telegram Mini App + WebApp + Telegram Bot.
* **Telegram Bot:** `@kreativaibot` (Havola: `https://t.me/kreativaibot`)
* **Asosiy Shior (Hero Heading):** *"Bilimingizni oshiring. AI va zamonaviy ko‘nikmalar orqali daromadingizni yangi bosqichga olib chiqing."*
* **Target Auditoriya:** SMM mutaxassislari, dizaynerlar, frilanserlar, tadbirkorlar, talabalar va sun'iy intellekt vositalaridan amaliyotda unumli foydalanmoqchi bo'lgan ijodkorlar.

---

## 2. ASOSIY IMKONIYATLAR VA FOYDALANUVCHI YO'LI (FEATURES & UX)

1. **⚡️ Telegram Mini App — 1 Bosishda Boshlash:**
   * Hech qanday murakkab ro'yxatdan o'tish yoki SMS kod kutish shart emas. Foydalanuvchi Telegram ichida bir marta bosish orqali avtomatik tizimga kiradi.
2. **📚 Yuqori Sifatli Amaliy Kurslar & Video Darslar:**
   * Qadamma-qadam video darsliklar, tayyor promptlar shablonlari, yuklab olinadigan materiallar va har bir dars bo'yicha amaliy topshiriqlar.
3. **🔐 Yopiq VIP Telegram Kanallariga Avtomatik Ulanish:**
   * Kurs sotib olingach, foydalanuvchiga faqat bitta akkaunt uchun mo'ljallangan bir martalik xavfsiz Telegram taklif havolasi (one-time invite link) beriladi. Begona shaxslar kira olmaydi.
4. **💳 O'zbekiston To'lov Tizimlari (Payme, Click, Uzcard, Humo):**
   * Chekni rasm sifatida yuklash va avtomatlashtirilgan superadmin tasdiqlovi. To'lov tasdiqlanganda bot darhol foydalanuvchiga xabar yuboradi.
5. **🏆 Verifikatsiyalangan QR-kodli Rasmiy Sertifikatlar:**
   * Kursni 100% muvaffaqiyatli tamomlagan talabalarga unikal ID va QR-kodga ega sertifikat avtomatik yaratiladi. Uni rezyumega qo'shish yoki tekshirish mumkin.
6. **🤖 24/7 AI Tutor (Sun'iy Intellekt Yordamchisi):**
   * O'quvchi dars davomida tushunmagan joylarini to'g'ridan-to'g'ri o'rnatilgan AI yordamchisidan so'rashi va darhol aniq javob olishi mumkin.
7. **📊 Shaxsiy O'quv Kabineti & Jonli Progress:**
   * Yakunlangan darslar soni, faol kurslar va umumiy foizli o'zlashtirish ko'rsatkichi.

---

## 3. USTOZLAR VA JAMOAT BO'LIMI (INSTRUCTORS / CREATORS)

### Jamoa Falsafasi:
> *"Kursni amaliy tajribaga ega, kontent, dizayn va AI vositalari bilan har kuni ishlaydigan ikki ustoz olib boradi. Siz faqat quruq nazariya emas, real loyihalarda sinalgan usullarni o‘rganasiz."*

### Bizning Maqsadimiz:
> *"Sizga AI’dan foydalanishni shunchaki tushuntirib berish emas, balki uni kontent, dizayn, biznes va shaxsiy loyihalaringizda real natijaga va daromadga aylantirishni o‘rgatish."*

---

### Ustoz 1: Zuhra Olimova
* **Mutaxassisligi:** SMM & AI Kontent eksperti
* **Unvoni / Tagline:** *"AI Formula" kursi asoschisi*
* **Statistika:**
  * **3+** yil tajriba
  * **9 000** obunachi (Instagram)
  * **130+** bitiruvchi
* **Tavsif (Bio):**
  * SMM va dizayn sohasida 3 yildan ortiq tajribaga ega mutaxassis. U AI yordamida rasm va video yaratish, kreativ kontent tayyorlash hamda sahifalarni rivojlantirish bo‘yicha faol ishlaydi.
  * Instagram’dagi [@zuhra_akbarovna](https://www.instagram.com/akbarovna_zuhra?igsi=MTh1dnRyeDhudW4xdg==) sahifasi egasi (9 000 obunachi). Telegram’dagi [@Olimova_Zuhra](https://t.me/+awu_1WFe7MhiNmQ6) kanalini yuritadi (1 500 obunachi).
  * "Hammaga Yetadi" kanalining avvalgi admini va qayta tiklanayotgan kanal egasi. "Dizayn ilk qadam" chellenji asoschisi (35+ ishtirokchi qatnashib, o'z biznesini yo'lga qo'ygan).
  * Sun’iy intellektda prompt yozish, rasm va video yaratishga bag‘ishlangan “AI Formula” kursi asoschisi.
* **Asosiy yo'nalishlari:**
  * `AI prompt yozish`
  * `Rasm va video yaratish`
  * `Sahifalarni rivojlantirish`
  * `SMM strategiya`
* **Ijtimoiy tarmoqlari:**
  * Instagram: `@zuhra_akbarovna`
  * Telegram: `@Olimova_Zuhra`
  * Kanal: `Hammaga Yetadi` (`https://t.me/hammagayetadi`)

---

### Ustoz 2: Yaxshi Bola
* **Mutaxassisligi:** Dasturlash & Dizayn ijodkori
* **Unvoni / Tagline:** *odobli.ai loyihasi asoschisi*
* **Statistika:**
  * **1,5+** yil tajriba
  * **9 000+** obunachi (Instagram)
  * **2 500+** o'quvchi
* **Tavsif (Bio):**
  * 22 yoshli Yaxshi Bola — dasturlash va dizayn yo‘nalishida 1,5 yildan ortiq tajribaga ega ijodkor. U Instagram’dagi [odobli.ai](https://www.instagram.com/odobli.ai?igsi=OWZlZnhiano2Z2x0) loyihasi asoschisi (9 000+ obunachi). Facebook’dagi sahifasini ham 1 000+ kishi kuzatadi.
  * U bir yil davomida “Hammaga Yetadi” kanalini yuritib, Canva va dizayn bo‘yicha foydali bilimlarni 2 500+ kishiga ulashgan.
  * Bu kursda u AI vositalarini amalda ishlatish, kreativ kontent yaratish, dizayn fikrlashi va raqamli loyihalarni rivojlantirish bo‘yicha o‘z tajribasini ulashadi.
* **Asosiy yo'nalishlari:**
  * `AI vositalarini amalda qo‘llash`
  * `Kreativ kontent yaratish`
  * `Dizayn fikrlashi`
  * `Raqamli loyihalarni rivojlantirish`
* **Ijtimoiy tarmoqlari:**
  * Instagram: `odobli.ai`
  * Facebook: `Facebook` (`https://www.facebook.com/share/1F1tuV2C9L/`)
  * Telegram: `@yomonboIa` (`https://t.me/yomonboIa`)

---

## 4. DIZAYN TIZIMI VA UI/UX TALABLARI (DESIGN SYSTEM)

* **Uslub:** Modern Apple & Linear Minimalist, Premium Glassmorphism, 3D elementlar, silliq animatsiyalar.
* **Ranglar palitrasi:**
  * **Cyan (Asosiy aksent):** `#06b6d4` / `#0891b2` (Innovatsiya, AI, yorug'lik)
  * **Violet (Ikkinchi aksent):** `#8b5cf6` / `#7c3aed` (Ijodkorlik, premiumlik)
  * **Gold (Mukofotlar & Yutuqlar):** `#f59e0b` / `#d97706`
  * **Ink / Dark Slate (Fon va Matnlar):** `#0f172a` (to'q matn), `#1e293b`
  * **Glass Effektlar:** `rgba(255, 255, 255, 0.7)` backdrop blur `16px`, nozik oq chegaralar (`border-white/20`).
* **Interaktivlik:**
  * Hover & Tap mikro-animatsiyalari (masalan, `active:scale-[0.98]`).
  * Ustozlar kartochkasini bosganda to'liq ma'lumotlar bilan chiroyli **Bottom Sheet** yoki **Modal** ochilishi.
  * Dinamik 21:9 aspektidagi banner slayderi (avtomatik aylanuvchi va swipe qo'llab-quvvatlovchi).

---

## 5. MAVJUD FRONTEND MANBA KODLARI (SOURCE CODE)

Quyida landing page yaratuvchi AI uchun loyihaning mavjud asosiy sahifalari va komponentlari kodlari keltirilgan:

### A) Bosh Sahifa Komponenti (`frontend/src/pages/HomePage.tsx`)
```tsx
import React, { useEffect, useRef, useState } from 'react';
import { motion, AnimatePresence } from 'motion/react';
import {
  ArrowRight,
  BookOpen,
  Trophy,
  Plus,
  GraduationCap,
  Award,
} from 'lucide-react';
import { Course, ContinueLearningData, Banner } from '../types';
import { CourseCard } from '../components/CourseCard';
import { InstructorsSection } from '../components/InstructorsSection';
import { useTelegram } from '../context/TelegramContext';
import { getToday } from '../utils/format';
import { api, toMediaUrl } from '../services/api';

interface HomePageProps {
  courses: Course[];
  banners: Banner[];
  bannersReady?: boolean;
  purchasedCourseIds?: Set<string>;
  purchasesLoading?: boolean;
  continueData?: ContinueLearningData | null;
  stats?: {
    completed_lessons_count: number;
    overall_progress_percent: number;
    enrolled_count: number;
  } | null;
  onSelectCourse: (course: Course) => void;
  onNavigateToCatalog: () => void;
  onNavigateToLearning: () => void;
}

const SLIDE_MS = 5200;

const container = {
  hidden: {},
  show: { transition: { staggerChildren: 0.07, delayChildren: 0.05 } },
};
const item = {
  hidden: { opacity: 0, y: 18 },
  show: { opacity: 1, y: 0, transition: { type: 'spring' as const, stiffness: 260, damping: 26 } },
};

export const HomePage: React.FC<HomePageProps> = ({
  courses,
  banners,
  bannersReady,
  purchasedCourseIds,
  purchasesLoading,
  continueData,
  stats,
  onSelectCourse,
  onNavigateToCatalog,
  onNavigateToLearning,
}) => {
  const { haptic, webApp } = useTelegram();
  const today = getToday();

  const [slideIdx, setSlideIdx] = useState(0);
  const [paused, setPaused] = useState(false);
  const touchX = useRef<number | null>(null);

  useEffect(() => {
    if (paused || banners.length < 2) return;
    const t = setTimeout(() => setSlideIdx((i) => (i + 1) % banners.length), SLIDE_MS);
    return () => clearTimeout(t);
  }, [slideIdx, paused, banners.length]);

  const activeBanner = banners.length > 0 ? banners[slideIdx % banners.length] : null;

  const handleBannerTap = (b: Banner) => {
    haptic?.impact?.('light');
    if (b.action_type === 'course' && b.action_value) {
      const course = courses.find(
        (c) => String(c.id) === b.action_value || c.slug === b.action_value
      );
      if (course) {
        onSelectCourse(course);
      } else {
        api.getCourseDetail(b.action_value).then(onSelectCourse).catch(() => {});
      }
    } else if (b.action_type === 'link' && b.action_value) {
      try {
        if (webApp?.openTelegramLink) webApp.openTelegramLink(b.action_value);
        else window.open(b.action_value, '_blank', 'noopener');
      } catch {
        window.open(b.action_value, '_blank', 'noopener');
      }
    }
  };

  const completedLessons = stats?.completed_lessons_count ?? 0;
  const enrolledCount = stats?.enrolled_count ?? 0;
  const overallProgress = stats?.overall_progress_percent ?? 0;

  const recommendedCourses = courses
    .filter((c) => c.id !== continueData?.course_id && !purchasedCourseIds?.has(c.id))
    .slice(0, 4);

  return (
    <motion.div
      variants={container}
      initial="hidden"
      animate="show"
      className="px-4 pt-4 space-y-5"
    >
      {/* 1. Sarlavha + Sana */}
      <motion.div variants={item} className="flex items-start justify-between">
        <div className="space-y-1">
          <p className="text-xs font-semibold text-cyan uppercase tracking-wider">Sizning o‘quv hududingiz</p>
          <h1 className="text-[30px] sm:text-[34px] font-extrabold text-slate-900 leading-tight tracking-tight">
            Bilimingizni<br />
            <em className="text-transparent bg-clip-text bg-gradient-to-r from-cyan-600 to-violet-600 not-italic">oshiring.</em>
          </h1>
        </div>

        <div className="bg-white/80 backdrop-blur-md border border-slate-200/80 rounded-2xl px-3 py-2 text-center min-w-[68px] shadow-sm">
          <span className="text-[20px] font-extrabold text-cyan-600 block leading-none tabular-nums">
            {today.day}
          </span>
          <span className="text-[11px] font-bold text-slate-800 block mt-0.5">
            {today.month}
          </span>
          <span className="text-[9px] text-slate-400 font-medium block">
            {today.weekday}
          </span>
        </div>
      </motion.div>

      {/* 2. Hero Banner Slayder */}
      {activeBanner && (
        <motion.section
          variants={item}
          className="relative w-full rounded-[24px] overflow-hidden shadow-xl"
        >
          <div className="relative w-full aspect-[21/9] bg-slate-100">
            <img
              src={toMediaUrl(activeBanner.image_url)}
              alt={activeBanner.title || 'Banner'}
              className="w-full h-full object-cover cursor-pointer"
              onClick={() => handleBannerTap(activeBanner)}
            />
            {activeBanner.title && (
              <div className="absolute inset-x-0 bottom-0 p-4 bg-gradient-to-t from-slate-950/80 to-transparent">
                <h2 className="text-[15px] font-bold text-white leading-snug">
                  {activeBanner.title}
                </h2>
              </div>
            )}
          </div>
        </motion.section>
      )}

      {/* 3. Statistika kartochkalari */}
      <motion.div variants={item} className="bg-white/80 backdrop-blur-md rounded-[20px] p-3 grid grid-cols-3 divide-x divide-slate-200/80 shadow-sm border border-slate-100">
        <div className="flex items-center space-x-2.5 px-1.5">
          <div className="w-9 h-9 rounded-xl bg-cyan-500/10 text-cyan-600 flex items-center justify-center">
            <BookOpen className="w-4 h-4" />
          </div>
          <div>
            <b className="text-[15px] font-extrabold text-slate-900 block leading-tight">{completedLessons}</b>
            <span className="text-[9px] text-slate-500 block">yakunlangan dars</span>
          </div>
        </div>

        <div className="flex items-center space-x-2.5 px-1.5">
          <div className="w-9 h-9 rounded-xl bg-violet-500/10 text-violet-600 flex items-center justify-center">
            <GraduationCap className="w-4 h-4" />
          </div>
          <div>
            <b className="text-[15px] font-extrabold text-slate-900 block leading-tight">{enrolledCount}</b>
            <span className="text-[9px] text-slate-500 block">aktiv kurs</span>
          </div>
        </div>

        <div className="flex items-center space-x-2.5 px-1.5">
          <div className="w-9 h-9 rounded-xl bg-amber-500/10 text-amber-600 flex items-center justify-center">
            <Trophy className="w-4 h-4" />
          </div>
          <div>
            <b className="text-[15px] font-extrabold text-slate-900 block leading-tight">{overallProgress}%</b>
            <span className="text-[9px] text-slate-500 block">umumiy progress</span>
          </div>
        </div>
      </motion.div>

      {/* 4. Mos Kurslar Ro'yxati */}
      <motion.div variants={item} className="space-y-3">
        <div className="flex items-center justify-between px-1">
          <h2 className="text-sm font-bold text-slate-900 flex items-center gap-1.5">
            <Award className="w-4 h-4 text-cyan-600" />
            Sizga mos kurslar
          </h2>
          <button onClick={onNavigateToCatalog} className="text-xs font-bold text-cyan-600 flex items-center">
            <span>Hammasi</span>
            <ArrowRight className="w-3.5 h-3.5 ml-0.5" />
          </button>
        </div>

        <div className="grid grid-cols-2 gap-3">
          {recommendedCourses.map((course, idx) => (
            <CourseCard
              key={course.id}
              course={course}
              onClick={() => onSelectCourse(course)}
              showTopBadge={idx === 0}
            />
          ))}
        </div>
      </motion.div>

      {/* 5. Ustozlar bo'limi */}
      <motion.div variants={item}>
        <InstructorsSection />
      </motion.div>
    </motion.div>
  );
};
```

---

### B) Ustozlar Bo'limi va Profil Modal Komponenti (`frontend/src/components/InstructorsSection.tsx`)
```tsx
import React, { useState } from 'react';
import { motion, AnimatePresence } from 'motion/react';
import {
  X,
  ChevronRight,
  Sparkles,
  Target,
  Instagram,
  Facebook,
  Send,
  Users,
} from 'lucide-react';

export interface Instructor {
  id: string;
  name: string;
  photo: string;
  role: string;
  tagline: string;
  accent: 'cyan' | 'violet';
  stats: { value: string; label: string }[];
  paragraphs: string[];
  highlights: string[];
  socials: { icon: any; label: string; url: string }[];
}

export const INSTRUCTORS: Instructor[] = [
  {
    id: 'zuhra-olimova',
    name: 'Zuhra Olimova',
    photo: '/images/ustoz_zuhra_olimova.webp',
    role: 'SMM & AI Kontent',
    tagline: '“AI Formula” kursi asoschisi',
    accent: 'violet',
    stats: [
      { value: '3+', label: 'yil tajriba' },
      { value: '9 000', label: 'obunachi' },
      { value: '130+', label: 'bitiruvchi' },
    ],
    paragraphs: [
      'Zuhra Olimova — SMM va dizayn sohasida 3 yildan ortiq tajribaga ega mutaxassis. U AI yordamida rasm va video yaratish, kreativ kontent tayyorlash hamda sahifalarni rivojlantirish bo‘yicha faol ishlaydi.',
      'Zuhra Instagram’dagi @zuhra_akbarovna sahifasi egasi bo‘lib, u yerda 9 000 ga yaqin obunachiga ega. Telegram’dagi @Olimova_Zuhra kanalini ham yuritadi; kanalda 1 500 ga yaqin obunachi bor.',
      'U “Hammaga Yetadi” kanalining avvalgi admini va qayta tiklanayotgan kanal egasi hisoblanadi. Zuhra “Dizayn ilk qadam” chellenji asoschisi. Unda 35+ ishtirokchi qatnashgan va ularning ko‘pchiligi olgan bilimlari asosida o‘z yo‘nalishi yoki biznesini yo‘lga qo‘ygan.',
      'Zuhra, shuningdek, sun’iy intellektda prompt yozish, rasm va video yaratishga bag‘ishlangan “AI Formula” bepul kursi asoschisi.',
    ],
    highlights: [
      'AI prompt yozish',
      'Rasm va video yaratish',
      'Sahifalarni rivojlantirish',
      'SMM strategiya',
    ],
    socials: [
      { icon: Instagram, label: '@zuhra_akbarovna', url: 'https://www.instagram.com/akbarovna_zuhra' },
      { icon: Send, label: '@Olimova_Zuhra', url: 'https://t.me/+awu_1WFe7MhiNmQ6' },
      { icon: Send, label: 'Hammaga Yetadi', url: 'https://t.me/hammagayetadi' },
    ],
  },
  {
    id: 'yaxshi-bola',
    name: 'Yaxshi Bola',
    photo: '/images/ustoz_yaxshi_bola.webp',
    role: 'Dasturlash & Dizayn',
    tagline: 'odobli.ai loyihasi asoschisi',
    accent: 'cyan',
    stats: [
      { value: '1,5+', label: 'yil tajriba' },
      { value: '9 000+', label: 'obunachi' },
      { value: '2 500+', label: "o'quvchi" },
    ],
    paragraphs: [
      '22 yoshli Yaxshi Bola — dasturlash va dizayn yo‘nalishida 1,5 yildan ortiq tajribaga ega ijodkor. U Instagram’dagi odobli.ai loyihasi asoschisi (9 000+ obunachi). Facebook’dagi sahifasini ham 1 000+ kishi kuzatadi.',
      'U avval bir yil davomida “Hammaga Yetadi” kanalini yuritib, Canva va dizayn bo‘yicha foydali bilimlarni 2 500+ kishiga ulashgan. Kanal hozir qayta tiklanib, yangi bosqichda rivojlanmoqda.',
      'Bu kursda Yaxshi Bola sizga AI vositalarini amalda ishlatish, kreativ kontent yaratish, dizayn fikrlashi va raqamli loyihalarni rivojlantirish bo‘yicha o‘z tajribasini ulashadi.',
    ],
    highlights: [
      'AI vositalarini amalda qo‘llash',
      'Kreativ kontent yaratish',
      'Dizayn fikrlashi',
      'Raqamli loyihalarni rivojlantirish',
    ],
    socials: [
      { icon: Instagram, label: 'odobli.ai', url: 'https://www.instagram.com/odobli.ai' },
      { icon: Facebook, label: 'Facebook', url: 'https://www.facebook.com/share/1F1tuV2C9L/' },
      { icon: Send, label: '@yomonboIa', url: 'https://t.me/yomonboIa' },
    ],
  },
];

export const InstructorsSection: React.FC = () => {
  const [active, setActive] = useState<Instructor | null>(null);

  return (
    <section className="space-y-3 pb-6">
      <div className="flex items-center justify-between px-1">
        <h2 className="text-sm font-bold text-slate-900 flex items-center gap-1.5">
          <Users className="w-4 h-4 text-violet-600" />
          Kurs mualliflari
        </h2>
        <span className="text-[11px] font-semibold text-slate-400">2 ta ekspert</span>
      </div>

      <div className="grid grid-cols-2 gap-3">
        {INSTRUCTORS.map((ins) => (
          <div
            key={ins.id}
            onClick={() => setActive(ins)}
            className="group relative bg-white/80 backdrop-blur-md rounded-2xl p-3 border border-slate-200/80 shadow-sm hover:shadow-md transition-all cursor-pointer flex flex-col items-center text-center"
          >
            <div className="w-16 h-16 rounded-full overflow-hidden mb-2.5 border-2 border-slate-100 shadow-inner">
              <img src={ins.photo} alt={ins.name} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300" />
            </div>
            <h3 className="text-sm font-bold text-slate-900 leading-tight">{ins.name}</h3>
            <p className="text-[11px] font-semibold text-cyan-600 mt-0.5">{ins.role}</p>
            <span className="text-[10px] text-slate-400 mt-1 line-clamp-1">{ins.tagline}</span>
            <div className="mt-2.5 flex items-center text-[10px] font-bold text-slate-700 bg-slate-100/80 rounded-lg px-2 py-1">
              Batafsil <ChevronRight className="w-3 h-3 ml-0.5" />
            </div>
          </div>
        ))}
      </div>

      {/* Profil Bottom Sheet / Modal */}
      <AnimatePresence>
        {active && (
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            className="fixed inset-0 z-50 bg-slate-950/60 backdrop-blur-sm flex items-end sm:items-center justify-center p-0 sm:p-4"
            onClick={() => setActive(null)}
          >
            <motion.div
              initial={{ y: '100%' }}
              animate={{ y: 0 }}
              exit={{ y: '100%' }}
              transition={{ type: 'spring', damping: 25, stiffness: 300 }}
              onClick={(e) => e.stopPropagation()}
              className="bg-white w-full max-w-lg rounded-t-[28px] sm:rounded-[28px] max-h-[85vh] overflow-y-auto p-5 space-y-4 shadow-2xl"
            >
              <div className="flex items-center justify-between border-b pb-3">
                <div className="flex items-center space-x-3">
                  <img src={active.photo} alt={active.name} className="w-12 h-12 rounded-full object-cover border" />
                  <div>
                    <h3 className="font-bold text-slate-900">{active.name}</h3>
                    <p className="text-xs text-slate-500">{active.role}</p>
                  </div>
                </div>
                <button onClick={() => setActive(null)} className="p-2 rounded-full bg-slate-100 text-slate-500">
                  <X className="w-4 h-4" />
                </button>
              </div>

              {/* Statistika */}
              <div className="grid grid-cols-3 gap-2 bg-slate-50 rounded-xl p-2.5 text-center">
                {active.stats.map((s, idx) => (
                  <div key={idx}>
                    <b className="text-sm font-bold text-slate-900 block">{s.value}</b>
                    <span className="text-[10px] text-slate-400">{s.label}</span>
                  </div>
                ))}
              </div>

              {/* Bio matni */}
              <div className="space-y-2 text-xs text-slate-600 leading-relaxed">
                {active.paragraphs.map((p, idx) => (
                  <p key={idx}>{p}</p>
                ))}
              </div>

              {/* Ko'nikmalar chiplari */}
              <div className="flex flex-wrap gap-1.5 pt-2">
                {active.highlights.map((h, idx) => (
                  <span key={idx} className="text-[11px] font-medium bg-slate-100 text-slate-700 rounded-lg px-2.5 py-1 flex items-center gap-1">
                    <Sparkles className="w-3 h-3 text-amber-500" />
                    {h}
                  </span>
                ))}
              </div>

              {/* Ijtimoiy tarmoq tugmalari */}
              <div className="flex gap-2 pt-2 border-t">
                {active.socials.map((soc, idx) => {
                  const Icon = soc.icon;
                  return (
                    <a
                      key={idx}
                      href={soc.url}
                      target="_blank"
                      rel="noopener noreferrer"
                      className="flex-1 py-2.5 rounded-xl bg-slate-100 hover:bg-slate-200 text-slate-800 font-semibold text-xs flex items-center justify-center gap-1.5 transition-colors"
                    >
                      <Icon className="w-3.5 h-3.5" />
                      {soc.label}
                    </a>
                  );
                })}
              </div>
            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>
    </section>
  );
};
```

---

### C) Kurs Kartochkasi Komponenti (`frontend/src/components/CourseCard.tsx`)
```tsx
import React from 'react';
import { Clock, BookOpen, Star, Flame } from 'lucide-react';
import { Course } from '../types';
import { formatUZS } from '../utils/format';
import { toMediaUrl } from '../services/api';

interface CourseCardProps {
  course: Course;
  onClick: () => void;
  showTopBadge?: boolean;
}

export const CourseCard: React.FC<CourseCardProps> = ({
  course,
  onClick,
  showTopBadge = false,
}) => {
  const isFree = !course.price || course.price === 0;

  return (
    <div
      onClick={onClick}
      className="group relative bg-white/90 backdrop-blur-md rounded-[20px] p-3 border border-slate-200/80 shadow-sm hover:shadow-md transition-all active:scale-[0.98] cursor-pointer flex flex-col justify-between"
    >
      <div>
        {/* Kurs Muqovasi (Cover) */}
        <div className="relative aspect-[16/10] rounded-2xl overflow-hidden mb-2.5 bg-slate-100">
          <img
            src={toMediaUrl(course.cover_url)}
            alt={course.title}
            className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
            onError={(e) => {
              (e.currentTarget as HTMLImageElement).src = '/images/course_default.jpg';
            }}
          />
          {showTopBadge && (
            <div className="absolute top-2 left-2 bg-rose-500 text-white text-[9px] font-bold px-2 py-0.5 rounded-full flex items-center gap-1 shadow-sm">
              <Flame className="w-3 h-3" /> TOP
            </div>
          )}
          {course.level && (
            <div className="absolute bottom-2 left-2 bg-slate-950/60 backdrop-blur-sm text-white text-[9px] font-semibold px-2 py-0.5 rounded-md">
              {course.level}
            </div>
          )}
        </div>

        {/* Sarlavha va qisqa tavsif */}
        <h3 className="text-[13px] font-bold text-slate-900 leading-snug line-clamp-2">
          {course.title}
        </h3>
        <p className="text-[11px] text-slate-500 line-clamp-2 mt-1">
          {course.short_description || course.description}
        </p>
      </div>

      {/* Narx va meta ma'lumotlar */}
      <div className="mt-3 pt-2.5 border-t border-slate-100 flex items-center justify-between">
        <div>
          {isFree ? (
            <span className="text-xs font-black text-emerald-600 uppercase">Bepul</span>
          ) : (
            <span className="text-xs font-extrabold text-slate-900">{formatUZS(course.price)}</span>
          )}
        </div>
        <div className="flex items-center space-x-1 text-[10px] font-bold text-amber-500">
          <Star className="w-3 h-3 fill-amber-400" />
          <span>{course.rating ? course.rating.toFixed(1) : '5.0'}</span>
        </div>
      </div>
    </div>
  );
};
```

---

## 6. AI GA BERISH UCHUN TAYYOR PROMPT (PROMPT TEMPLATE FOR LANDING PAGE AI)

Yangi Landing page yaratuvchi AI ga quyidagi ko'rsatmani berishingiz mumkin:

```markdown
Siz dunyodagi eng tajribali Senior Frontend / UI-UX dizayner va Landing Page mutaxassisisiz.
Menga "Kreativ AI" (Telegram Mini App Kurslar Platformasi) uchun zamonaviy, konversiyasi yuqori, Apple va Linear uslubidagi ajoyib Landing Page (yoki yangilangan WebApp taqdimot sahifasi) yasab bering.

Talablar:
1. Hero bo'limi: Kuchli sarlavha, jozibador 3D/AI tasvirlar, "Telegram orqali boshlash" (CTA) tugmasi, bugungi sana va statistika chipi.
2. Afzalliklar bo'limi (Features/Bento Grid): 
   - Telegram Mini App (1 bosishda kirish)
   - Amaliy video darslar & shablonlar
   - VIP Yopiq Telegram kanallar
   - QR-kodli rasmiy sertifikatlar
   - 24/7 AI Yordamchi (AI Tutor)
   - O'zbekiston to'lov tizimlari (Payme/Click)
3. Kurslar katalogi: Filtrlar, kurs kartochkalari, narxlar, reytinglar va darajalar.
4. Ustozlar bo'limi: Zuhra Olimova va Yaxshi Bola haqida batafsil ma'lumotlar, ularning statistikasi, fotosuratlari va ijtimoiy tarmoq tugmalari.
5. Fikrlar & Ijtimoiy Isbot (Testimonials & Social Proof): Bitiruvchilar natijalari va fikrlari.
6. FAQ & Yakuniy CTA: Savol-javoblar va darhol Telegram botga o'tish tugmasi (`https://t.me/kreativaibot`).
7. Texnologiyalar: React, Tailwind CSS, Lucide Icons, Framer Motion (yoki Motion).
```
